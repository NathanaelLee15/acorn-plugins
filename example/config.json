{
    "name": "Example",
    "description": "Sanity Check.",
    "tools": [
        {
            "name": "",
            "path": "{plugin_name}/{tool_name}.js",
            "schema": {
                "name": "get_weather",
                "description": "Get the current weather in a given location",
                "input_schema": {
                    "type": "object",
                    "properties": {
                        "location": {
                            "type": "string",
                            "description": "The city and state, e.g. San Francisco, CA"
                        }
                    },
                    "required": ["location"]
                }
            }
        }
    ]
}